{% extends 'base.html.twig' %}

{% block title %}Accueil - Watson{% endblock %}

{% block body %}
<div class="bg-light">
    {# Hero Section #}
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm py-3">
        <div class="container">
            <ul class="collapse navbar-collapse" id="navbarNav">
                <li class="navbar-nav p-2"><a class="navbar-brand fw-bold" href="{{ path('app_home_page') }}">Watson</a></li>
                <li class="navbar-nav me-auto">
                    <a href="{{ path('app_link_index') }}" class="btn btn-outline-secondary">Voir les liens</a>
                </li>
                <li class="navbar-nav ms-auto">
                    <a class="btn btn-secondary" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            </ul>
        </div>
    </nav>

    {# Main Hero Content #}
    <section class="py-5">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <h1 class="display-4 fw-bold">Bienvenue sur Watson</h1>
                    <p class="lead text-muted">Votre plateforme de gestion de liens</p>
                    <div class="d-grid gap-2 d-md-flex">
                    </div>
                </div>
                <div class="col-lg-6">
                    <img src="https://via.placeholder.com/500x400" class="img-fluid rounded" alt="">
                </div>
            </div>
        </div>
    </section>
{% endblock %}
